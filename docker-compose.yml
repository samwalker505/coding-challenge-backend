version: "3"
services:
  redis:
    image: redis
  app:
    links:
      - redis
    depends_on:
      - redis
    container_name: finddoc-backend
    restart: always
    build: ./
    ports:
      - "3000:3000"
    volumes:
      - ./src:/app/src
